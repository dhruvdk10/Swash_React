import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// Use HashRouter inside App, no changes needed here
ReactDOM.createRoot(document.getElementById("root")).render(
  <App />
);

